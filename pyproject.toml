[project]
name = "mirai"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0.0",
    "gradio>=4.0.0",
    "numpy>=1.24.0",
    "sympy>=1.12",
    "matplotlib>=3.7.0"
]
[tool.setuptools.packages.find]
where = ["src"]