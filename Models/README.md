## Simple model training scripts to create a variety of models to test on the debugger

This directory contains some example scripts for training simple models to train and test on the debugger, currently, there are some simple MLP models, ResNet models all with a variety of configurations.
